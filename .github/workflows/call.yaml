name: Call
on:
  push:

jobs:
#   call:
#     name: Call
#     uses: dysnix/github-workflows/.github/workflows/docker.build-and-push.yaml@switch-to-latestv2

  output:
    name: Output
    runs-on: ubuntu-latest
#     needs: [call]
    steps:
      - run: |
          echo
      - name: Scan Docker Repository
        uses: dhet/scan-docker-tags-action@v1.0.1
        with:
          image: library/alpine
          max-age-minutes: 1440000
          tag-regex: 3\.15\.\d+
          username: dennybaa
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        id: scan

      - name: Check outputs
        run: |
          echo "Latest Image Tag - ${{ steps.scan.outputs.tags }}"
