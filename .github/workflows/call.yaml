name: Call
on:
  push:

jobs:
#   call:
#     name: Call
#     uses: dysnix/github-workflows/.github/workflows/docker.build-and-push.yaml@switch-to-latestv2

  output:
    name: Output
    runs-on: ubuntu-latest
#     needs: [call]
    steps:
      - run: |
          echo
      - name: Get latest tag
        id: latest_tag
        uses: jacobtomlinson/gha-get-docker-hub-tags@0.1.1
        with:
          org: _
          repo: alpine

      - name: Check outputs
        run: |
          echo "Latest Image Tag - ${{ steps.latest_tag.outputs.tag }}"
